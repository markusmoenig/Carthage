// JavaScript game object logic

// Put your initialization code here

require("CT.Math")

print(object.name + " starting up!")

// Called on every frame, remove if not needed.
function tick(time) {
}
